<% if !logged_in? %>
<h1>Sign up</h1>
<%= render 'form' %>
<% else %>
<%User.all.each do |user| %>
    <h1 class=user-name><strong><%=link_to user.full_name, user_path(user.id)%></strong></h1>
    <%end %>
<%end %>